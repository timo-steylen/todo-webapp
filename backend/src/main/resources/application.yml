spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/webapp
    username: todo
    password: todo

  spring:
    flyway:
      enabled: true
    jpa:
      hibernate:
        ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.flywaydb: DEBUG